# ══════════════════════════════════════════════════════════════════
#  FlightTrack v3 — Environment Variables
#  Add in Vercel: Settings → Environment Variables → Redeploy
# ══════════════════════════════════════════════════════════════════

# ── PRIMARY APIs (add both for best results) ──────────────────────

# Apify — Google Flights data via actor (replaces SerpAPI)
# Sign up free: https://console.apify.com → Settings → Integrations → API token
# Free: $5 platform credit EVERY month (~400-600 searches at typical actor pricing)
APIFY_TOKEN=apify_api_your_token_here
# Optional: which actor to run (username~actor-name). Default mirrors the
# SerpAPI response shape. Browse: https://apify.com/store?search=google%20flights
# APIFY_GFLIGHTS_ACTOR=skootle~google-flights-scraper

# Duffel — NDC airline fares (often cheaper than GDS prices)
# Sign up free: https://app.duffel.com/join → Developers → Access tokens
# Free test mode, no credit card
DUFFEL_ACCESS_TOKEN=duffel_test_your_token_here

# ── FALLBACK ONLY (optional — only used if SerpAPI + Duffel not set) ──

# Anthropic — Claude AI agent with web search
# Only activates when NO other flight APIs are configured
# If you have SerpAPI + Duffel, you do NOT need this key
# Get from: https://console.anthropic.com → API Keys
ANTHROPIC_API_KEY=sk-ant-your-key-here

# ── EMAIL ALERTS (optional) ───────────────────────────────────────

# Resend — sends email when price drops below your alert threshold
# Sign up free: https://resend.com → API Keys (100 emails/day free)
RESEND_API_KEY=re_your_key_here
ALERT_FROM_EMAIL=FlightTrack <onboarding@resend.dev>
# SECURITY: comma-separated list of allowed alert recipients.
# Without this, /api/notify is a public endpoint anyone can use to spam
# from your Resend account. Strongly recommended on a public Vercel URL.
ALERT_ALLOWED_EMAILS=you@example.com

# ── DEPRECATED ─────────────────────────────────────────────────────
# KIWI_API_KEY — Kiwi.com closed Tequila to the public in 2024; new keys
# are invitation-only. The adapter still works if you hold a legacy key.
# TRAVELPAYOUTS_TOKEN — Aviasales cached prices (free affiliate token)
TRAVELPAYOUTS_TOKEN=your_travelpayouts_token
